{
  "name": "nexora",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "echo 'ERROR: This is a monorepo root - deploy via render.yaml (Blueprint) instead. See DEPLOY_RENDER.md.' && exit 1",
    "lint": "node scripts/lint.mjs",
    "validate:render": "python3 scripts/validate_render.py",
    "test": "npm run lint && npm run validate:render && npm --workspaces --if-present test",
    "build:images": "node scripts/build-images.mjs"
  },
  "workspaces": [
    "services/*",
    "packages/*",
    "backend"
  ]
}
